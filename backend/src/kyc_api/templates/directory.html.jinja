{% extends "base.html.jinja" %}

{% block title %}Annuaire des personnes — Know Your Candidate{% endblock %}

{% block content %}
  <h1>Annuaire des personnes</h1>
  <p>
    Toutes les personnes présentes dans nos données : celles issues du référentiel de l'Assemblée
    nationale, et les candidat·es à la présidentielle 2027 que nous suivons, qu'elles aient siégé
    ou non. Voir <a href="/methodologie">la méthodologie</a>.
  </p>

  <form class="search-form" action="/personnes" method="get" role="search">
    <div>
      <label for="q">Rechercher par nom</label>
      <input type="search" id="q" name="q" value="{{ q or '' }}"
             hx-get="/fragments/personnes" hx-target="#annuaire" hx-swap="innerHTML"
             hx-trigger="input changed delay:300ms, search" hx-include="#legislature"
             hx-push-url="true">
    </div>
    <div>
      <label for="legislature">Législature</label>
      <select id="legislature" name="legislature"
              hx-get="/fragments/personnes" hx-target="#annuaire" hx-swap="innerHTML"
              hx-trigger="change" hx-include="#q" hx-push-url="true">
        <option value="" {{ "selected" if not legislature else "" }}>Toutes</option>
        {% for leg in [15, 16, 17] %}
          <option value="{{ leg }}" {{ "selected" if legislature == leg else "" }}>{{ leg }}e législature</option>
        {% endfor %}
      </select>
    </div>
    <button type="submit">Rechercher</button>
  </form>

  <div id="annuaire">
    {% include "_directory_list.html.jinja" %}
  </div>
{% endblock %}
