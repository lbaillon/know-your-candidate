[project]
name = "kyc-api"
version = "0.1.0"
description = "Know Your Candidate — API et interface web (FastAPI, lecture seule sur PostgreSQL)"
readme = "README.md"
requires-python = ">=3.14,<3.15"
dependencies = [
    "fastapi>=0.141.1,<0.142",
    "uvicorn[standard]>=0.52.1,<0.53",
    "jinja2>=3.1.6,<4",
    "asyncpg>=0.31.0,<0.32",
    "pydantic>=2.13.4,<3",
    "pydantic-settings>=2.15.0,<3",
    "markdown>=3.10.3",
    # Échange de jeton OAuth GitHub (D3.1, docs/plans/phase-3-categorisation.md) : le seul appel
    # HTTP sortant du backend, à la connexion admin. N'était jusqu'ici qu'un outil de test (voir
    # dependency-groups.dev ci-dessous) ; il devient du code de production.
    "httpx>=0.28.1,<0.29",
    # Exigé par SessionMiddleware de Starlette pour signer le cookie de session admin.
    "itsdangerous>=2.2.0,<3",
    # Exigé par FastAPI pour lire un formulaire POST et un fichier déposé (back-office et import,
    # phase 3). Sans lui, tout le back-office rend un 500 au premier envoi.
    "python-multipart>=0.0.20,<0.1",
]

[dependency-groups]
dev = [
    "ruff>=0.16.2,<0.17",
    "ty>=0.0.70",
    "pytest>=9.1.1,<10",
    "pytest-asyncio>=1.4.0,<2",
    "lxml>=6.1.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/kyc_api"]

[tool.ruff]
line-length = 100
target-version = "py314"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]
# B008 : faux positif sur `Depends(...)` en valeur par défaut, qui est l'idiome FastAPI standard
# pour l'injection de dépendances (voir routers/*.py).
ignore = ["B008"]

[tool.ruff.lint.isort]
known-first-party = ["kyc_api"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
